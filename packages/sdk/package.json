{
  "name": "@agentgate/sdk",
  "version": "0.1.0",
  "description": "AgentGate client for agents. NOT PUBLISHED: no npm release exists, and `private: true` is deliberate — this is a prototype with no versioning policy behind it. Consume it from this workspace, or vendor dist/. The wire contract it wraps (POST /v1/proxy) is documented in the OpenAPI document and is what to write against if you would rather not depend on unreleased code.",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "prepare": "tsc -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "typecheck": "pnpm --filter \"@agentgate/sdk^...\" build && tsc -p tsconfig.test.json",
    "test": "pnpm --filter \"@agentgate/sdk^...\" build && vitest run"
  },
  "devDependencies": {
    "@agentgate/auth": "workspace:*",
    "@agentgate/gateway": "workspace:*",
    "@agentgate/mock-github": "workspace:*",
    "@agentgate/policy": "workspace:*",
    "@agentgate/shared": "workspace:*",
    "@types/node": "^22.19.2",
    "dotenv": "^17.4.2",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
